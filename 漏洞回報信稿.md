# 漏洞回報信稿

**收件人**：chein@mail.ntou.edu.tw（陳立原 組長）
**副本**：ryanhuang@mail.ntou.edu.tw（黃榮彥 資訊專員）
**主旨**：教學務系統登入回應含明文密碼之資安問題回報

---

陳組長、黃專員 您好：

我是資訊工程學系學生 [你的姓名]（學號 [你的學號]）。近期在自己的帳號上研究
教學務系統（ais.ntou.edu.tw）的登入流程時，發現一個可能影響全校使用者的問題，
在此回報。

## 問題

登入時 POST 到 `Default.aspx` 的**回應 HTML 中，包含使用者剛送出的密碼明文**，
且出現兩次：

```html
<script>var keyObj = {LoginPWD:'<使用者輸入的密碼>'};</script>
...
<script>_i(0, 'LoginPWD').value = '<使用者輸入的密碼>';</script>
```

登入成功或失敗都會出現。

## 重現方式

1. 開啟瀏覽器開發者工具的 Network 頁籤
2. 於 ais.ntou.edu.tw 正常登入
3. 檢視對 `Default.aspx` 的 POST 回應內容，搜尋 `LoginPWD`

## 影響

密碼以明文形式出現在回應內容中，代表它會存在於：

- **瀏覽器 DOM** —— 該頁面上執行的任何 script（含瀏覽器擴充功能）皆可讀取
- 開發者工具的 Network 紀錄
- 任何中間的 HTTP proxy、快取或存取紀錄
- 使用者「另存新檔」保存的頁面

密碼在此處以明文出現並非登入所必需，前端已無需再次取得使用者輸入的密碼。

## 建議

移除回應中回填密碼的兩處 script。若原本是為了「登入失敗時保留使用者輸入」，
可改為只保留帳號欄位，或於前端不清空欄位即可，不需由伺服器回填。

## 補充說明

- 此問題是我在**自己的帳號**上操作時發現的，未使用他人帳號、未嘗試存取任何
  他人資料，亦未對系統進行壓力測試或其他干擾。
- 相關筆記存放於我個人的 **private** 儲存庫，**未公開**，也不會在貴組確認修復前
  對外揭露。
- 若需要更詳細的技術細節、封包內容或協助驗證修復結果，我都很樂意配合。

謝謝您們維護系統的辛勞。

[你的姓名] 敬上
[聯絡信箱]
[聯絡電話]

---

## 寄出前檢查

- [ ] 把 `[你的姓名]`、`[你的學號]`、`[聯絡信箱]`、`[聯絡電話]` 填上
- [ ] **信裡不要附上你的真實密碼** —— 上面用的是佔位符，保持這樣
- [ ] 不要附上程式碼或儲存庫連結（他們需要的話會問；先寄最小必要資訊）
- [ ] 用學校信箱寄，比較容易被當一回事

## 之後

- 對方回覆通常需要幾個工作天。沒回音的話一週後再客氣地追一次。
- 確認修復前，`ntou-app` 儲存庫維持 private。
- 修復後再決定要不要轉公開。

.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# ---- 個資：預設全部不進版控 ----
# fixture 是從真實帳號抓下來的頁面。login.py 存檔時已經洗過
# （明文密碼、學號、姓名、身分證、IP、VIEWSTATE），但預設仍然不上傳 ——
# 洗過的東西沒人檢查過第二遍，而漏掉一次的代價是密碼外流。
#
# 真的需要讓測試在 CI 跑的話，逐個檔案檢查後再 git add -f，
# 並且務必先跑 spike/check.py。
spike/fixtures/*.html

# callback 的原始回應（debug 用），一定含 session 相關資料
spike/fixtures/callbacks/

# 這兩個是選單結構，沒有個資，刻意保留：
!spike/fixtures/menu_tree.json

# ---- 個人文件，不進版控 ----
# 漏洞回報的信稿：內容描述一個尚未修復的弱點。即使 repo 是私有的，
# 一旦哪天轉公開，刪檔案也救不回 git 歷史。這種東西留在工作區就好。
漏洞回報*.md
zeroday-submission.md

# ---- TDX 金鑰，不進版控 ----
# 交通部 TDX 的 client id / secret。放在檔案裡是為了不用每次重開機都重設
# 一次環境變數 —— flutter build 和 spike/tdx.py 都讀這一份。
#
# 這把金鑰的層級跟學校密碼完全不同：最壞的下場是配額被別人用掉，
# 所以放在本機檔案是可以接受的。真正該藏的東西走的是 Keystore。
# 但它一樣不該進 git —— 進去就跟著原始碼被推上 GitHub 了。
# 用萬用字元是因為「複製一份來改」很自然（Windows 會產生
# 「tdx.local.json - 複製.example」這種名字），而那種檔名不會被
# 單一檔名的規則擋到 —— 裡面卻是同一把金鑰。
tdx.local*
!tdx.local.json.example

# Claude Code：本機專屬設定（含機器路徑、裝置序號），不進版控
.claude/settings.local.json
